Created a sample project with Postgres as db and echo and go mod
created a docker file for docker image build and startup.sh file to run the build
Docker Command file is added for docker commands use to build and deploy the container 

Sprint Planing
1) setup the project with go mod, echo and sqlx
2) create a search api with route, controller, service and dao packages
3) setup db config with Postgres
4) Setup docker file and build startup.sh script 